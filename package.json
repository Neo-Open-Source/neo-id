{
  "name": "neo-id",
  "version": "4.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "test:e2e": "turbo test:e2e",
    "lint": "turbo lint",
    "db:migrate": "pnpm --filter @neo-id/db migrate",
    "db:seed": "pnpm --filter @neo-id/db seed",
    "db:generate": "pnpm --filter @neo-id/db generate"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  }
}
